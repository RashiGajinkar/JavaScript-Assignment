const names = ["Momo", "Sana", "Jihyo", "Mina","Tzuyu"];
        
        console.log(`Here the names names are ${names}`);
        console.log(`Now the reverse order of names are ${names.reverse()}`)